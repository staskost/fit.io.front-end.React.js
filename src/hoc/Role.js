// Used as an enum to reference valid app roles
const Role = {
    User: 'user',
    Trainer: 'trainer',
    Admin: 'admin',
    Guest: 'guest' // our convention for any user not logged in
}

export default Role;